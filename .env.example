# AWS
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=

# Terraform variables (used when running terraform plan/apply)
TF_VAR_project_name=mt-ingest
TF_VAR_ingest_job_image=
# For list variables use a .tfvars file, e.g. ecs_subnet_ids = ["subnet-xxx"], ecs_security_group_ids = ["sg-xxx"]
# TF_VAR_scheduler_timezone=Europe/London

# For local runs of the ingest job (optional)
# CONFIG_KEY=tenants/example/salesforce.yaml
# S3_CONFIG_BUCKET=
