# AWS (for local dev and ECR push)
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=

# Terraform variables (source before: terraform plan/apply)
# Required:
TF_VAR_project_name=mt-ingest
TF_VAR_ingest_job_image=123456789012.dkr.ecr.us-east-1.amazonaws.com/mt-ingest:latest
# ecs_subnet_ids and ecs_security_group_ids: use terraform.tfvars (see terraform/terraform.tfvars.example)

# Optional Terraform (defaults shown):
# TF_VAR_aws_region=us-east-1
# TF_VAR_scheduler_timezone=UTC

# Local run of the ingest job (optional; needs AWS creds for S3 and Secrets Manager)
# CONFIG_KEY=tenants/example/salesforce.yaml
# S3_CONFIG_BUCKET=mt-ingest-pipeline-configs-123456789012
